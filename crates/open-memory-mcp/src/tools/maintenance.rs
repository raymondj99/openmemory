//! Maintenance tools (`open_memory_consolidate`). Lands in commit 34.

use crate::tools::Entry;

#[allow(clippy::ptr_arg)]
pub(crate) fn register_all(_out: &mut Vec<Entry>) {
    // Tools land in commit 34.
}
