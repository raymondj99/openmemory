//! Streamable-HTTP MCP transport. Implementation lands in commit 35; this
//! file is only here so the `mcp-http` feature flag has a target to gate.

#![allow(dead_code)]

use crate::OpenMemoryMcpServer;

/// Placeholder — real impl in commit 35.
pub fn placeholder(_server: OpenMemoryMcpServer) {}
