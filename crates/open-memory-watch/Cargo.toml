[package]
name = "open-memory-watch"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "open-memory: filesystem watcher with incremental re-indexing"
repository.workspace = true

[lints]
workspace = true

# The watcher writes to `MetadataStore` and the FTS5 index unconditionally,
# so its `open-memory-index` and `open-memory-graph` dependencies pull in
# `sqlite` + `fts5` directly. This means the crate has no opt-in features
# of its own — `cargo build -p open-memory-watch` with no flags is the
# single supported configuration. Higher-level crates (notably the CLI)
# gate the watcher behind their own `watch` feature so a slim build can
# omit it entirely; once present, however, it is fully wired.

[dependencies]
open-memory-core = { workspace = true }
open-memory-index = { workspace = true, default-features = false, features = ["fts5"] }
open-memory-graph = { workspace = true, default-features = false, features = ["fts5"] }

# File watching + walking
notify = { workspace = true }
notify-debouncer-full = { workspace = true }
ignore = { workspace = true }
walkdir = { workspace = true }

# Hashing + IDs
blake3 = { workspace = true }

# Diagnostics
thiserror = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
open-memory-core = { workspace = true, features = ["testing"] }
open-memory-graph = { workspace = true, features = ["testing"] }
tempfile = { workspace = true }
